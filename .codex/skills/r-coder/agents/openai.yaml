interface:
  display_name: "R Coder"
  short_description: "Write one R script in the repo's house style"
  default_prompt: "Write or revise one R script in this repository. Follow `R Code Conventions.md` and match the style of neighboring scripts in `scripts/`."
